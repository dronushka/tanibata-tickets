"use client"

import { DashboardOrder } from "@/types/types"

export default function DashboardOrderForm ({ order }: { order: DashboardOrder}) {
    console.log(order)
    return <p>Dashboard Order Form</p>
}